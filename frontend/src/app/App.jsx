//import React from 'react';
import AppRoutes from './routes';
 
/**
 * Root application component.
 *
 * Renders the top-level route configuration for TransitHub_JU.
 * Acts as the entry point mounted by `main.jsx`.
 *
 * @returns {JSX.Element} The application's route tree.
 */
function App() {
  return <AppRoutes />;
}
 
export default App;